@model TaskUser.ViewsModels.StockViewsModels.StockViewModels



@{

    Layout = "_Layout";
}
<div class="container">
    @if (ViewData["AddFailure"] != null)
    {
        <div class="alert alert-info"style="width: 200px">
            <strong>@ViewData["AddFailure"]</strong>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">x</button>
        </div>
 
    }
    <form method="post" asp-controller="Stock" enctype="multipart/form-data" asp-action="Create">
        <legend>@StockLocalizer.GetLocalizedString("lbl_AddStock")</legend>
        @*<input type="hidden" class="form-control" asp-for="Id"  placeholder="Your name" required>*@
        <div class="form-group col-xs-6">
            <label>@StockLocalizer.GetLocalizedString("lbl_StoreId")</label>
            <select class="form-control" asp-for="StoreId" asp-items="@ViewBag.StoreId">
                @* <option asp-for="StoreId" >@ViewBag.StoreId[""]</option> *@
            </select>
            
            <span asp-validation-for="StoreId"></span>
        </div>
        <div class="form-group col-xs-6">
            <label>@StockLocalizer.GetLocalizedString("lbl_ProductId")</label>
            <select class="form-control" asp-for="ProductId" asp-items="@ViewBag.ProductId"></select>
            <span asp-validation-for="ProductId"></span>
        </div>
        <div class="form-group col-xs-6">
            <label>@StockLocalizer.GetLocalizedString("lbl_Quantity")</label>
            <input type="text" class="form-control" asp-for="Quantity" placeholder="Quantity" >
            <span asp-validation-for="Quantity"></span>
        </div>
        <div class="form-group col-xs-12">
            <button type="submit" class="btn btn-primary btn-lg btn-block">
                @StockLocalizer.GetLocalizedString("btn_AddNewStock")
            </button>
        </div>
    </form>
    @* <div class="form-group col-xs-12"> *@
    @*     <a class="nav-link text-dark" asp-area="" asp-controller="Store" asp-action="Back"> *@
    @*         <img src="~/img/iconback.png" alt="Smiley face" height="42" width="42"> *@
    @*         @Localizer["Go back to the main page"] *@
    @*     </a> *@
    @* </div> *@
</div>
              

